import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bryanberendsen.lastworkout',
  appName: 'Last Workout app',
  webDir: 'dist/lw-frontend/browser'
};

export default config;
